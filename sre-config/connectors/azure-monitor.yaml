# Azure Monitor Connector — ARM connector format
# Always created by configure-sre-agent.ps1 as the incident source
# Reference: PUT {agentResourceId}/connectors/azure-monitor?api-version=2025-05-01-preview
api_version: azuresre.ai/v2
kind: DataConnector
spec:
  name: azure-monitor
  dataConnectorType: AzureMonitor
  dataSource: azure-monitor
